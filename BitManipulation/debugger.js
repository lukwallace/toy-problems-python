// Explain what the following code does: (( n & (n - 1)) === 0)

// My answer:
// It checks if n is a number that only contains one 1. 

// Actual anser:
// checks if then number is a power of two (containing only one 1 visual result);